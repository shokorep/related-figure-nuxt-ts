name: scheduletest
on:
  schedule:
    - branches:
      - githubactions
    - cron:  '46 */1 * * *'

jobs:
  # audit
  audit:
    name: audit
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: npm i
    - run: npm audit
# Todo Test
